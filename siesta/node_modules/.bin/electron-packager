#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@electron-forge/maker-base/node_modules/electron-packager/bin/electron-packager.js" "$@"
  ret=$?
else 
  node  "$basedir/../@electron-forge/maker-base/node_modules/electron-packager/bin/electron-packager.js" "$@"
  ret=$?
fi
exit $ret
